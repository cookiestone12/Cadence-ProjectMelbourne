REPLIT PROMPT — Add Dropbox-Linked Audio + AI Audio Analysis Layer (NO breaking changes)

Context:
You are working inside an existing app: “Rythm — Catalog Intelligence” (multi-tenant) with modules including Catalog, Works, Releases, Contracts, Royalties, Placements, Reports, Valuation, Action Items, Creator Roster. Do NOT change existing features, routes, UI layouts, or remove/rename any existing fields. This task is ONLY to add a new layer of features that integrates Dropbox storage and AI audio analysis, and makes analysis results sortable/filterable for sync briefs and A&R needs. Keep everything backwards compatible. Reference the existing structure: songs have a detail modal with tabs (Details/Health/Credits/DSP Links/Contracts/Rights & Splits), releases have readiness checks + exports, creators have profiles with catalog + accounting, royalties ingest statements and match to catalog, etc.  [oai_citation:0‡Rythm — Catalog Intelligence.pdf](sediment://file_000000001fa871f5a00dab0af9e5579e)

Goal:
Add Dropbox API integration so a user can link audio files to:
- Songs (recordings)
- Releases (tracklist items)
- Creator profiles (optional: as an “asset library” per creator)
Then run AI audio analysis on linked files and store only derived features + tags to enable:
- Sorting/filtering by musical attributes (BPM, key, energy, mood, genre-ish vectors, “sync tags”)
- Pulling “brief-ready” playlists based on criteria (e.g., “uplifting indie folk, 95–110 BPM, female vocal, warm acoustic, tension build, 30s edit available”)
Do NOT require hosting audio on our servers long-term. Use Dropbox as the source-of-truth storage and only download temporarily for analysis.

Non-negotiables:
- No breaking changes to current workflows.
- No permanent audio storage in our DB/filesystem (unless explicitly optional and off by default).
- Respect multi-tenant isolation: Dropbox links and analyses are scoped to an Organization.
- Secure OAuth flow; never store raw Dropbox user passwords; store tokens encrypted.
- Analysis jobs must be asynchronous (queue/worker). UI must remain snappy.
- If Dropbox is disconnected, nothing breaks; links show as unavailable and analysis actions disabled.

High-level features to add:
1) Dropbox Connector
   - “Settings” -> add a new tab: “Integrations” (or within Settings add “Dropbox” section)
   - OAuth connect/disconnect
   - Choose a default Dropbox folder per Organization (optional)
   - Allow per-song file linking from the Song Detail Modal
   - Allow per-release linking (bulk attach to tracks in a release)
   - Support multiple audio files per song (main mix, instrumental, clean, stems zip) but clearly label file type

2) Audio Attachments
   - Add a new tab in Song Detail Modal: “Audio”
     - List linked files with: filename, type (Main/Instrumental/Clean/Alt Mix/Stems), duration (if known), Dropbox path, “Analyze” button, last analyzed timestamp, status
     - “Link from Dropbox” button opens file picker (Dropbox Chooser OR our own picker via Dropbox API)
     - “Unlink” button (does not delete from Dropbox)
   - Releases: in Release Builder, add a section “Audio Links” that shows each track and whether it has linked audio; allow bulk link/match by filename conventions.
   - Creator profile: optional “Audio Library” subtab showing all audio attachments across their songs, searchable/filterable.

3) AI Audio Analysis (non-hosted)
   - When user clicks “Analyze” (or bulk analyze), create a job:
       a) fetch a temporary download link from Dropbox
       b) stream/download the file to ephemeral storage
       c) run analysis pipeline
       d) store derived features + tags
       e) delete temp file
   - Must support “Analyze all linked audio in Release” and “Analyze all linked audio for Creator”
   - Store only derived data; never store full audio bytes.

4) Derived Features + Tags (the “worthwhile” layer)
   Persist a normalized feature set that enables brief matching and sorting:
   Core technical:
   - BPM (tempo) + confidence
   - Key (e.g., C#m) + confidence
   - Time signature (optional)
   - Duration
   - Loudness (LUFS), peak, dynamic range
   - Intro length / first vocal time (optional but valuable for sync)
   Structure cues (lightweight):
   - Energy curve (low/med/high segments)
   - “Has big chorus” heuristic (best-effort)
   - “Stinger ending” / “button ending” heuristic
   Content descriptors:
   - Mood tags (e.g., uplifting, melancholic, tense, dreamy)
   - Texture tags (acoustic, electronic, gritty, lush, intimate)
   - Vocal presence (instrumental vs vocal; male/female/duet if feasible; otherwise “vocal present” only)
   - Genre vector (embedding-based, not hard genre labels)
   Sync utility tags:
   - “Trailer-ish”, “dramedy”, “romance”, “sports”, “luxury”, “youth”, etc. (generated by model + user editable)
   Similarity:
   - Embedding vector for “sounds-like” search within the org
   User curation:
   - Let users override/add tags manually; AI tags are suggestions with confidence scores.

5) Search & Filtering UX (minimal changes)
   - Catalog table: add optional columns (toggleable) for BPM, Key, Mood, Energy, “Audio Linked”, “Analyzed”
   - Add new filters in Catalog/Works/Placements search:
     - Audio linked yes/no
     - Analyzed yes/no
     - BPM range slider
     - Key dropdown
     - Mood/texture tag multi-select
     - Vocal present toggle
     - “Stems available” toggle (if a stems file is linked)
   - Add a new page (or subtab under Reports) called “Brief Builder”
     - Input: free-text brief + structured filters
     - Output: ranked list of songs with match reasons + quick actions (create playlist/export PDF with links)
   Keep UI consistent with existing patterns (tables, tabs, modals).

Architecture requirements:
A) Data model additions (don’t alter existing tables; extend with new ones)
- IntegrationAccount
  - id, org_id, provider = 'dropbox'
  - access_token_encrypted, refresh_token_encrypted (if applicable)
  - token_expires_at
  - connected_by_user_id
  - default_folder_path (optional)
  - created_at, updated_at

- AudioAsset
  - id, org_id
  - song_id nullable, release_id nullable, creator_id nullable (at least song_id primary; release linking via song/release join)
  - provider = 'dropbox'
  - provider_file_id (Dropbox file id)
  - path_display, name, size_bytes
  - file_type_enum: MAIN, INSTRUMENTAL, CLEAN, ALT_MIX, STEMS, OTHER
  - mime_type
  - duration_seconds (nullable)
  - checksum/hash (nullable)
  - last_verified_at, is_available_cached
  - created_at, updated_at

- AudioAnalysis
  - id, org_id, audio_asset_id
  - status: QUEUED/RUNNING/SUCCEEDED/FAILED
  - analyzed_at
  - bpm, bpm_confidence
  - key, key_confidence
  - lufs, peak_db, dynamic_range
  - vocal_present boolean + confidence (or nullable)
  - embeddings_vector (store in vector DB or pgvector if available)
  - features_json (for additional metrics)
  - error_message
  - created_at, updated_at

- AudioTag
  - id, org_id
  - name
  - tag_type: MOOD, TEXTURE, SYNC, USER
  - created_by_user_id nullable (system vs user)
  - created_at

- AudioAssetTag
  - audio_asset_id, audio_tag_id
  - source: AI or USER
  - confidence float nullable
  - created_at

B) Job/Queue
- Add a background worker for analysis jobs.
- Rate-limit analysis per org to avoid cost explosions.
- Allow cancel/retry.
- Store job progress and surface status in UI.

C) Security
- OAuth 2.0 with Dropbox. Encrypt tokens at rest.
- Ensure org scoping on every query and job.
- Use least privilege scopes: read-only if possible; if using chooser, still store minimal.
- Use signed temporary links from Dropbox; never expose permanent links publicly.
- Audit log entry: “Linked audio”, “Unlinked audio”, “Ran analysis”, “Bulk analysis run”.

D) Performance
- Do not block UI while analyzing.
- Cache lightweight metadata (duration, availability).
- Store embeddings for fast “sounds-like” search.

E) Failure modes
- If Dropbox token expires, prompt reconnect, do not crash.
- If file missing, mark unavailable and create an Action Item (“Audio link broken”).
- If analysis fails, mark failed with reason and allow retry.

Integrations UX details:
- Settings -> Integrations -> Dropbox
  - Connect button
  - Connected account display
  - Default folder picker
  - “Test connection”
  - Disconnect

Action Items integration:
- Auto-generate tasks when:
  - Song has no audio linked but is marked “Ready” for release (optional rule)
  - Release moved to “Ready” but tracks missing audio links (optional)
  - Audio link broken
  - Analysis recommended (e.g., “New audio linked, not analyzed”)

Brief Builder logic:
- Accept natural language prompt; extract constraints (tempo, mood, vocal, genre vibe)
- Use hybrid ranking:
  1) hard filters (BPM range, vocal present, analyzed)
  2) semantic match over tag set + embeddings
  3) boost by placements history and health score (existing signals)
- Output includes:
  - why it matched (tags/features)
  - audio link button (Dropbox share link or internal player if feasible)
  - export options (PDF/CSV of shortlist with links)

AI Analysis implementation guidance:
- Use a modular pipeline:
  - metadata extraction (duration, sample rate)
  - tempo/key estimation
  - loudness metrics
  - embeddings + tag generation (LLM/classifier) from audio features and optional lyrics (if available in system)
- Make it pluggable so we can swap models/vendors later.
- IMPORTANT: No copyrighted-music “style cloning” features; analysis is for classification/metadata, not generation.

Deliverables:
1) Schema migrations for new tables
2) Dropbox OAuth connect/disconnect + token storage
3) Song Detail Modal “Audio” tab + link/unlink UI
4) Release-level bulk linking UI (filename matching helper)
5) Background analysis worker + job tracking UI
6) Tagging system (AI + user tags)
7) Catalog filters + Brief Builder page
8) Action Items hooks for broken links + unanalyzed audio

Do not:
- Replace existing navigation or redesign pages.
- Rename existing entities or remove existing exports.
- Require users to host audio in Rythm.
- Add complicated new pricing screens. (Just implement features; pricing is separate.)

Acceptance checklist:
- User can connect Dropbox at org level.
- User can link a Dropbox audio file to a song.
- User can bulk link audio to tracks in a release.
- User can run analysis and see BPM/key/mood/tags stored.
- User can filter catalog by BPM, key, mood, “audio linked/analyzed”.
- Brief Builder can generate a shortlist + export.
- Nothing else in the app breaks or changes behavior.

Build it now.