{
  "name": "cadence-catalog-intelligence",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "html2canvas": "^1.4.1"
  }
}
